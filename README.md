I have created a Python class for a webpage (e.g., LoginPage) and tested it for if wrong credentials are entered by the user it should throw an error
I have successfully tested using pytest and playwright automation framework
I have handled writing the selectors, organizing the architecture, debugging a string mismatch, fixing a typo, and getting the final confirmation.
Attached an image of successfully running cmd
